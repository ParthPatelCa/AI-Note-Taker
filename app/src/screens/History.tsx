import { View, Text } from "react-native";

export default function History() {
  return (
    <View style={{ flex:1, padding:24 }}>
      <Text style={{ fontSize:18 }}>History list will render here in the next commit.</Text>
    </View>
  );
}
