import { View, Text } from "react-native";

export default function Summary() {
  return (
    <View style={{ flex:1, padding:24 }}>
      <Text style={{ fontSize:18 }}>Saved summary will render here in the next commit.</Text>
    </View>
  );
}
